<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Information"></activity>
        <activity android:name=".ChatBot" />
        <activity android:name=".Answer3" />
        <activity android:name=".Answer2" />
        <activity android:name=".Answer1" />
        <activity
            android:name=".ui.login.Information"
            android:label="@string/title_activity_information" />
        <activity android:name=".Chatbot" />
        <activity android:name=".CreateMFaculty" />
        <activity android:name=".EntranceActivity" />
        <activity android:name=".CreateYourAvatarName" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>